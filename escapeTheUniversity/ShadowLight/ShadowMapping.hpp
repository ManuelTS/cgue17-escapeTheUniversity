#pragma once

// Contains all information needed for the renderLoop.cpp to perform shadow mapping with PCF
class ShadowMapping
{
	private:
	public:
		ShadowMapping();
		~ShadowMapping();
};
